name: Qodana
on:
  workflow_dispatch:
  pull_request:
  push:
    branches: # Specify your branches here
      - main # The 'main' branch
      - 'releases/*' # The release branches

jobs:
  qodana:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      checks: write
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.sha }}  # to check out the actual pull request commit, not the merge commit
          fetch-depth: 0  # a full history is required for pull request analysis
      - name: 'Qodana Scan'
        uses: JetBrains/qodana-action@v2026.1
        with:
          pr-mode: false
          upload-result: true
          # Baseline: Bestandsbefunde von main werden nicht als 'new' gemeldet.
          # Auffrischen: Lauf mit entferntem --baseline starten, das Artefakt
          # 'qodana-report' herunterladen und dessen qodana.sarif.json hier ersetzen.
          args: --baseline qodana.sarif.json
        env:
          QODANA_TOKEN: ${{ secrets.QODANA_TOKEN_852197924 }}
          QODANA_ENDPOINT: 'https://qodana.cloud'